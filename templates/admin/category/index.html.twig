{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <h1 class="flex justify-center mb-8">Liste des catégories</h1>
    <a class="ml-32 font-semibold hover:font-bold" href="{{ path('app_admin') }}" >Retour</a>
        <div class="grid grid-cols-3 gap-4 p-4">
        {% for category in categories %}
            <div class=" flex flex-col items-center">
                <p class="font-semibold">{{ category.categorie }}</p>
                <p>
                    {# <a href="{{ path('admin_products_show', {'id': product.id}) }}">voir</a>
                    <a href="{{ path('admin_products_edit', {'id': product.id}) }}">modifier</a> #}
                </p>
            </div>
        {% else %}
            no records found
        {% endfor %}
        </div>

    <a class="ml-32 pb-10 font-semibold hover:font-bold" href="{{ path('admin_products_new') }}">Ajouter un produit</a>
{% endblock %}
